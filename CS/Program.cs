using System; 

namespace MyVisualApp // 2. Namespace (Container for code)
{
    public class Program // 3. Class (The blueprint)
    {
        // 4. Main Method (The entry point)
        public static void Main(string[] args)
        {
            string message = "Hello, C# World!"; // 5. Variable Declaration
            
            Console.WriteLine(message); // 6. Method Call
        }
    }
}